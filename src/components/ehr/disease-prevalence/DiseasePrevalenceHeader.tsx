import React from 'react';

interface DiseasePrevalenceHeaderProps {
    onExportReport?: () => void;
}

const DiseasePrevalenceHeader: React.FC<DiseasePrevalenceHeaderProps> = ({ onExportReport }) => {
    // Header is now handled in the main page component
    return null;
};

export default DiseasePrevalenceHeader;
